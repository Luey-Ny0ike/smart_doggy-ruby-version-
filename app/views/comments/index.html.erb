<p id="notice"><%= notice %></p>

<div class="container">
  <div class="well">
    <h1>Comments</h1>
    <hr>
    <% @comments.each do |comment| %>
    <p>
      <em><%= comment.user.first_name %></em>:
      <%= comment.comment %>
      <% if comment.user == current_user %>
      <%= link_to 'Edit', edit_testimonial_comment_path(@testimonial, comment) %>
      <%= link_to 'Destroy', testimonial_comment_path(@testimonial, comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <% end %>
  <% end %>

    <div class="panel panel-default">
      <div class="panel-body">
        <div class="row">
          <div class="col-md-6">
            <%= link_to 'New Comment', new_testimonial_comment_path(@testimonial) %>
          </div>
          <div class="col-md-6">
            <p>like</p>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>


<br>

<%= link_to 'New Comment', new_testimonial_comment_path %>
